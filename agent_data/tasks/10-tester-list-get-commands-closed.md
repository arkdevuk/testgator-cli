# 10 — `tester list` / `tester get` commands

**Depends on:** 07

## Summary

Tester read commands, following the pattern established in task 07.

## Scope

- `testgator-cli tester list` — list testers, with a `--project <id>` filter if the API supports it.
- `testgator-cli tester get <id>` — single tester detail.

## Acceptance criteria

- Unit + functional tests following task 06/07's pattern.
